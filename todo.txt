exit | exit
export -> affichage dans ordre ascii
export a -> crée une valeur dans l’env qui est affichée par export mais pas env
export USER -> ne change pas la valeur déjà présente
$?=1 est présent dans l’environnement des enfants
leaks quand on exit
makefile de printf
